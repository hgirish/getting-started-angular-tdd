export class MockSquareRootService {
    calculateSquareRoot(value:number):number{
        // perform predefined square root calculation based on the input value
        return Math.sqrt(value);
    }
}